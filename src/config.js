/**
 * Provides wallet configuration options.
 */
export class Config {

  // Provider network (e.g. kovan homestead rinkeby ropsten).
  static providerName = '';

  // JSON-RPC URL the provider is connected to.
  static jsonRpcProviderUrl = '';
}
